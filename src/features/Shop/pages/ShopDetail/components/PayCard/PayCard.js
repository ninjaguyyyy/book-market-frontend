import React, { Component } from "react";
import PropTypes from "prop-types";

export default class PayCard extends Component {
    // static propTypes = {
    //     prop: PropTypes,
    // };

    render() {
        console.log(React.version);
        return <div>pay card</div>;
    }
}
